name: xcode_parser1
description: Reading and analyzing the contents of .pbxproj files. Modifying project settings and configurations. Saving changes to .pbxproj files.
version: 1.2.1
repository: https://github.com/ivine/xcode_parser
homepage: https://github.com/ivine/xcode_parser

environment:
  sdk: ^3.3.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  collection: ^1.18.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
  mockito: ^5.0.0

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:
